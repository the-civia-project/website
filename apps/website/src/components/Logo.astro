---
import LogoSVG from '@the-civia-project/assets/logo.svg';

interface Props {
  size?: 'small' | 'big';
}

const { size = 'big' } = Astro.props;

const size_px = size === 'big' ? 64 : 32;
---

<img
  src={LogoSVG.src}
  alt="The Civia Project"
  width={size_px}
  height={size_px}
/>
